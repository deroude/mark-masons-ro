type: object
properties:
  id:
    type: integer
    format: int64
  user:
    $ref: ./User.yaml
  value:
    description: Value
    type: number
    format: double
  paymentDate:
    description: Payment Date
    type: string
    format: date
  status:
    description: Status
    type: string
    enum: [SENT,VERIFIED]