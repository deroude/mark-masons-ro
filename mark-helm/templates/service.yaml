apiVersion: v1
kind: Service
metadata:
  name: {{ include "mark-helm.fullname" . }}
  labels:
    {{- include "mark-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mark-helm.selectorLabels" . | nindent 4 }}
